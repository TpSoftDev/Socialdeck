// -----------------------------------------------------------------------------
// firebase_auth_email_links.dart
// -----------------------------------------------------------------------------
// URLs used by Firebase Auth email actions (e.g. password reset).
// The host must appear under Firebase Console → Authentication → Authorized domains.
// -----------------------------------------------------------------------------

/// Continue / action URL base for [ActionCodeSettings] when sending reset emails.
/// Project: socialdeck-dev — domain verified as authorized (.web.app).
const String kFirebaseAuthEmailContinueUrl = 'https://socialdeck-dev.web.app';
