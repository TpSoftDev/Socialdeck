import 'package:flutter/material.dart';

class SignUpConfirmPasswordPage extends StatelessWidget {
  const SignUpConfirmPasswordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Confirm Password Page'),
      ),
    );
  }
}